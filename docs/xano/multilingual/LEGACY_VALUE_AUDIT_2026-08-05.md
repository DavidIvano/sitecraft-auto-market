# Dry-run legacy-значений перед translation batch 01

Дата: 5 августа 2026 года. Workspace `115940`, live data source. Проверено 28 объявлений, включая значения в колонках и структурированные строки старых описаний. Контакты продавцов в отчёт не включены.

## Результат

- Публичных кандидатов: 13.
- Публичных кандидатов с неизвестными категориальными значениями: 0.
- Объявлений с подготовленными translation metadata до запуска: 1.
- Неизвестных значений во всей таблице: 2 уникальных значения в двух удалённых объявлениях.

| Поле | Неизвестное значение | Объявления | Решение |
|---|---|---:|---|
| `fuel_type` | `Не указано` | 48, 54 | Не преобразовывать молча; объявления удалены и исключены из batch |
| `transmission` | `Не указано` | 48, 54 | Не преобразовывать молча; объявления удалены и исключены из batch |

Все найденные публичные значения кузова, топлива, коробки передач, привода, цвета, состояния, типа продавца, типа транспорта и страны покрываются текущим `BACKEND_VALUE_CATALOG`, включая legacy-вариант `Черный`.

## Первый пакет

В batch включены три новейших публичных объявления: 96, 95 и 94. Используется SHA-256 от канонического JSON `car_listing_translation_source_v1`. Создано восемь идемпотентных задач: `de`, `uk`, `en` для объявлений 94 и 95; `uk`, `en` для объявления 96, поскольку его немецкий перевод уже завершён.

Исходные заголовки, описания и legacy-категории не изменялись. До записи сохранён rollback-снимок `migration-backups/2026-08-05-translation-batch-01.before.json`.
